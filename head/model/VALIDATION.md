# 算法目录重构验证记录

检查日期：2026-09-11。运行环境：本地 Conda `New`，Python 3.10.19，
PyTorch 2.1.2+cu121，模型推理使用 CPU，每进程 2 个 CPU 线程。
没有新建非 Conda 环境，没有上传权重、数据、运行缓存。

目录命名跟进：已统一为 `head/model/` 和 `adapter.py:Adapter`，真实权重回归
重新运行，87 项通过。下面四次闭环冒烟及旧版数值对照是前一次目录重构的记录，
并非本次改名后重新跑出的整套评测；本次没有改动网络计算、控制器或评价公式。

## 验证范围

| 检查 | 结果 |
| --- | --- |
| 自动化回归（包含显式提供两套真实权重的测试） | 87 项通过 |
| Pluto 与 WayFormer checkpoint 参数加载 | 通过 |
| 吉利、Waymo 输入适配及 21/80/115 帧调用（受场景长度限制） | 通过 |
| 新算法通过目录/模块约定接入，无核心模型分支修改 | 通过 |
| WayFormer 最高概率选轨迹、世界坐标转换、不把高斯参数当速度 | 通过 |
| Pluto 后轴参考点、六列输出转换、默认神经网络轨迹选择 | 通过 |
| 吉利 [T,1] 标量状态兼容、不修改源状态维度 | 通过 |
| Wheel 构建、包含两个模型/配置/许可证/evaluation、仓库外导入 | 通过 |
| evaluation 公式与公共控制器文件 | 本次未修改 |
| Pluto Waymo 样例对照旧发布版 | 奖励、帧数、事件和 evaluation_v2 完全一致 |
| WayFormer 未来记录扰动不变性 | 所查样例输出完全不变 |
| Pluto 未来记录扰动不变性 | **不成立：已确认旧版的日志辅助路线依赖** |

“87 项通过”包含确认旧版 Pluto 输入口径限制的测试，
**不等于 87 项严格无未来信息的验证通过**。
WayFormer 的旧版完整 Lightning 运行环境未安装在此 Conda 环境中，
因此不宣称它与旧完整环境逐帧闭环数值等价；已完成新接口的真实权重及闭环验证。

## 实际闭环冒烟测试

仍使用 HEAD 的 evaluation；均为单场景检查，不能用作测试集性能结论。
预热 21 帧、每 5 帧重规划、上限 120 帧，按原逻辑在到达终点、失败或场景结束时提前终止。
吉利日志的 scenario_id 重复为 3，实际检查的是本地压缩包顺序对应的场景文件，
不应将该 ID 当作跨文件唯一键。

| 模型 | 数据 | 总帧/评价帧 | score | collision | offroad | completion | TTC strict | comfort strict |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Pluto | Waymo | 62 / 41 | 1.0000 | 0 | 0 | 1 | 1 | 1 |
| Pluto | 吉利 | 120 / 99 | 0.5833 | 0 | 0 | 0 | 1 | 1 |
| WayFormer | Waymo | 90 / 69 | 0.0000 | 1 | 0 | 0 | 1 | 0 |
| WayFormer | 吉利 | 44 / 23 | 0.0000 | 1 | 0 | 0 | 1 | 1 |

四个运行均正常生成 metrics.json，runtime error 为 null。
WayFormer 的碰撞是所查驾驶表现，不被掩盖为“模型测试无问题”。
Pluto 与 WayFormer 的输入上下文不同：前者沿用日志辅助路线，
故上表不是严格同信息条件下的算法优劣比较。

## 发布口径确认

本次保留 Pluto 的旧路线估计，以免目录重构同时偷偷改变研究口径。
路线来自完整日志的自车轨迹，因此会使用未来位置。README 和运行警告已明确标记。
项目负责人已确认保持原逻辑不变。本次按旧口径发布，仅记录限制；
没有将其改成无未来信息输入，也不据此重写已有实验分数。

本记录只覆盖代码接入验证，不包含 171/512 场景全量重跑、SAE 实验或训练收益验证。
